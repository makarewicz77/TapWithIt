<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tapwithit">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:screenOrientation="portrait" android:name=".View.RegisterActivity"></activity>
        <activity android:screenOrientation="portrait" android:name=".View.ScoreBoardActivity" />
        <activity android:screenOrientation="portrait" android:name=".View.PregameActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".View.ClickerGameActivity"
            android:theme="@style/AppTheme" />
        <activity android:screenOrientation="portrait" android:name=".View.GameMenuActivity" />
        <activity android:screenOrientation="portrait" android:name=".View.LoginRegisterActivity" />
        <activity android:screenOrientation="portrait" android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>